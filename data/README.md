# data/

Dados de referência que **acompanham o programa** e são versionados: perfis por
modelo de IED, tabelas de parâmetros, listas de nomes válidos.

Não confundir com `library/` (acervo de arquivos do usuário, fora do git) nem
com `samples/` (exemplos para teste).

Ainda vazio.
