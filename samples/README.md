# samples/

Oscilografias de exemplo, usadas pelos testes.

**Só entram aqui arquivos sintéticos ou explicitamente liberados.** Uma
oscilografia real identifica instalação, data e comportamento da rede da
distribuidora — e este repositório é público.

Os arquivos reais do parque, para desenvolvimento do leitor, ficam fora do
repositório (o `.gitignore` não os cobre por nome; guarde-os em outra pasta).

Ainda vazio.
